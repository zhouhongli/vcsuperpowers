# 智能日志分析与诊断平台开发实战培训

## 副标题：基于 Claude Code 的全栈开发实践与最佳实践

**培训时长：** 90 分钟  
**目标受众：** 全栈开发者、AI 辅助开发实践者  
**培训形式：** 案例驱动 + 代码演示 + 互动讨论

---

## 目录

### 第一部分：项目概述与背景（5 页）
### 第二部分：技术架构与设计（5 页）
### 第三部分：核心功能实现（8 页）
### 第四部分：开发流程与最佳实践（6 页）
### 第五部分：问题排查与修复实战（4 页）
### 第六部分：经验教训与总结（2 页）

---

## 第一部分：项目概述与背景

### 第 1 页：培训目标

**学习目标：**
- 理解智能日志诊断平台的完整架构
- 掌握 AI 辅助开发的最佳实践模式
- 学会全栈 Bug 溯源的方法论
- 能够在本项目中应用 TDD 和上下文管理策略

**培训结束后你将能够：**
- 独立开发类似的全栈应用
- 使用 AI 工具高效完成开发任务
- 避免本课程中踩过的所有坑

---

### 第 2 页：项目背景

**为什么需要这个平台？**

| 痛点 | 传统方式 | 本平台方案 |
|-----|---------|-----------|
| 日志分散 | 登录多台服务器 grep | 统一 Web 界面查看 |
| 诊断依赖专家 | 人工分析堆栈 | 规则 + 大模型自动诊断 |
| 问题重复发生 | 没有历史参考 | 相似日志推荐 |
| 严重程度难判断 | 主观经验 | 基于规则的客观评估 |

**核心价值主张：**
- ⚡ 5 分钟内完成日志提交到诊断的全流程
-  自动诊断准确率 > 85%（规则匹配高置信度场景）
- 📊 可视化仪表盘实时监控异常趋势

---

### 第 3 页：需求分析要点

**需求收集方法：**
1. 利益相关者访谈（运维团队、开发团队）
2. 现有工作流观察（日志排查流程）
3. 竞品分析（Splunk、ELK Stack 功能对比）

**核心需求矩阵：**

| 功能模块 | 优先级 | 复杂度 | 开发工时 |
|---------|-------|-------|---------|
| 日志 CRUD | P0 | 低 | 4h |
| 规则诊断引擎 | P0 | 中 | 6h |
| 仪表盘图表 | P1 | 中 | 4h |
| 大模型降级 | P1 | 高 | 4h |
| 文件上传 | P2 | 低 | 2h |

**需求确认 Checkpoint：** 与用户逐项确认功能边界，避免范围蔓延

---

### 第 4 页：项目约束条件

**技术约束：**
- 前端：纯 HTML/CSS/JS + Bootstrap 5（无构建步骤）
- 后端：Python 3.11+ FastAPI
- 数据库：Mock 模拟（预留 Repository 接口）
- 部署：Docker 容器化

**非功能需求：**
- 响应时间：API < 200ms（诊断 < 2s）
- 并发：支持 10 个并发用户
- 可用性：99% 内部 SLA

**明确排除的范围：**
- ❌ 用户认证（内部工具，网络隔离）
- ❌ 实时日志流（离线分析场景）
- ❌ 告警通知（专注于诊断，非监控）

---

### 第 5 页：项目时间线

```
迭代 1: Scaffold + Mock DB        ████████░░░░░░░░  完成
迭代 2: FastAPI 基础 API           ████████░░░░░░░░  完成
迭代 3: 日志 CRUD 完整实现         ████████░░░░░░░░  完成
迭代 4: 诊断引擎（规则匹配）        ████████░░░░░░░░  完成
迭代 5: 前端表单 + 列表页          ████████░░░░░░░░  完成
迭代 6: 仪表盘 + 诊断详情          ████████░░░░░░░░  完成
迭代 7: Docker + 集成测试          ████████░░░░░░░░  完成
补充实现：文件上传/编辑/LLM/错误页  ████████░░░░░░░░  完成
```

**总开发周期：** 1 天（AI 辅助开发）  
**代码行数：** ~2000 行（前后端合计）

---

## 第二部分：技术架构与设计

### 第 6 页：系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    智能日志分析与诊断平台                    │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐              ┌──────────────┐            │
│  │   Frontend   │              │    Backend   │            │
│  │  (Bootstrap) │  ◄────────►  │   (FastAPI)  │            │
│  │ - 日志表单   │    REST      │ - REST API   │            │
│  │ - 仪表盘     │    JSON      │ - 诊断引擎   │            │
│  │ - 详情页     │              │ - Repository │            │
│  └──────────────┘              └──────┬───────┘            │
│                                       │                     │
│                                       ▼                     │
│                              ┌───────────────┐              │
│                              │ Mock Database │              │
│                              │ (预留 DB 接口)  │              │
│                              └───────────────┘              │
└─────────────────────────────────────────────────────────────┘
```

**架构决策记录（ADR）：**
- ADR-001: 为什么选择前后端分离？ → 独立部署、技术栈解耦
- ADR-002: 为什么用 Mock DB？ → 快速迭代，后期可替换真实存储

---

### 第 7 页：前端架构

**技术选型：**
- Bootstrap 5.3.2：UI 组件库（无需编译，CDN 引入）
- Chart.js 4.x：数据可视化
- 原生 JavaScript：无框架依赖

**目录结构：**
```
frontend/
├── index.html         # 首页（欢迎页）
├── submit.html        # 提交日志表单
├── list.html          # 日志列表页
├── dashboard.html     # 仪表盘
├── diagnosis.html     # 诊断详情页
├── edit.html          # 编辑日志页
├── 404.html / 500.html # 错误页面
├── css/
│   └── style.css      # 自定义样式
└── js/
    ├── api.js         # API 调用封装
    ├── submit.js      # 提交页逻辑
    ├── list.js        # 列表页逻辑
    ├── dashboard.js   # 仪表盘逻辑
    ├── diagnosis.js   # 诊断页逻辑
    └── edit.js        # 编辑页逻辑
```

**关键设计决策：**
- 单页应用 vs 多页应用？ → 选择多页（简单、SEO 友好、无构建）
- 状态管理？ → 无需（每页独立，通过 URL 参数传递状态）

---

### 第 8 页：后端架构

**分层架构：**
```
routes/          # API 端点定义（HTTP 层）
    ├── logs.py      # 日志 CRUD 端点
    └── diagnosis.py # 诊断端点

services/        # 业务逻辑层
    ├── diagnosis.py      # 诊断服务（规则 +LLM）
    ├── diagnosis_rules.py # 规则匹配引擎
    └── diagnosis_llm.py  # 大模型降级模块

models/          # 数据模型
    └── log_entry.py     # LogEntry 数据类

repository/      # 数据访问层
    └── log_repository.py # Mock 实现（预留 DB 接口）
```

**设计模式应用：**
- Repository 模式：数据访问抽象
- Strategy 模式：规则匹配 vs 大模型降级
- Single Responsibility：每层职责单一

---

### 第 9 页：数据模型设计

**LogEntry 模型：**
```python
class LogEntry:
    id: str              # UUID
    content: str         # 日志内容（必填）
    exception_type: str  # 异常类型（5 选 1）
    severity: str        # LOW/MEDIUM/HIGH/CRITICAL
    occurred_at: Optional[datetime]
    service_name: Optional[str]
    stack_trace: Optional[str]
    user_id: Optional[str]
    created_at: datetime
    updated_at: datetime
```

**Diagnosis 模型：**
```python
class Diagnosis:
    id: str              # UUID
    log_id: str          # 外键
    root_cause: str      # 根因分析
    solution: str        # 建议解决方案
    severity_assessment: str  # 评估严重程度
    similar_logs: List[str]   # 相似日志 ID 列表
    created_at: datetime
```

**数据完整性约束：**
- 日志内容不能为空
- 异常类型必须在预定义枚举中
- 诊断结果与日志一对一关联

---

### 第 10 页：API 端点设计

| 方法 | 端点 | 描述 | 状态码 |
|-----|------|------|-------|
| POST | /api/logs | 创建日志（支持文件上传） | 201 |
| GET | /api/logs | 获取列表（分页/筛选） | 200 |
| GET | /api/logs/{id} | 获取详情 | 200/404 |
| PUT | /api/logs/{id} | 更新日志 | 200/404 |
| DELETE | /api/logs/{id} | 删除 | 204/404 |
| DELETE | /api/logs | 批量删除 | 204 |
| POST | /api/logs/{id}/diagnose | 一键诊断 | 201/404 |
| GET | /api/logs/{id}/diagnosis | 获取诊断结果 | 200/404 |
| GET | /api/dashboard/stats | 仪表盘统计 | 200 |

**API 设计规范：**
- RESTful 风格
- JSON 响应格式统一
- 错误响应：`{"detail": "错误消息"}`
- 分页参数：`?page=1&page_size=20`

---

## 第三部分：核心功能实现

### 第 11 页：文件上传功能（后端）

**实现要点：**
```python
# 双模式检测：通过 Content-Type 区分
if content_type.startswith("multipart/form-data"):
    # 文件上传模式
    file = await request.form()
    content = await file["file"].read()
    content = content.decode("utf-8", errors="replace")
else:
    # JSON 模式
    data = await request.json()
    content = data.get("content", "")
```

**文件验证：**
- 扩展名：.log / .txt
- 大小限制：≤ 5MB（返回 413）
- 编码：UTF-8（失败返回 400）

**测试覆盖：**
- ✅ 正常文件上传成功
- ✅ 非日志扩展名拒绝
- ✅ 超大文件拒绝（413）
- ✅ 非 UTF-8 编码处理

---

### 第 12 页：文件上传功能（前端）

**FormData 提交：**
```javascript
async function handleSubmit() {
    const formData = new FormData();
    const file = fileInput.files[0];
    const content = contentTextarea.value;
    
    // 二选一验证
    if (!file && !content.trim()) {
        showToast('请上传文件或输入日志内容', 'warning');
        return;
    }
    
    if (file) {
        formData.append('file', file);
    } else {
        formData.append('content', content);
    }
    
    // 提交
    const log = await logsApi.createFromFile(formData);
    window.location.href = `diagnosis.html?id=${log.id}`;
}
```

**用户体验优化：**
- 文件选择后显示文件名
- 拖拽上传支持（可选扩展）
- 上传进度条（大文件场景）

---

### 第 13 页：规则诊断引擎

**诊断流程：**
```
1. 提取异常类型 + 日志内容 + 堆栈跟踪
          ↓
2. 规则匹配（keywords + exception_type）
          ↓
3. 计算置信度 score
          ↓
4. score >= 0.7？──是──→ 返回规则诊断结果
          │
         否
          │
          ▼
5. 降级到大模型 API（Claude）
          ↓
6. 解析 LLM 响应，返回诊断结果
```

**规则示例：**
```python
{
    "name": "NullPointerException",
    "exception_type": "NullPointerException",
    "keywords": ["null", "is null", "cannot invoke"],
    "root_cause_template": "对象未初始化或为 null：{matched_info}",
    "solutions": [
        "检查对象初始化逻辑",
        "添加 null 检查",
        "使用 Optional 包装",
    ],
}
```

---

### 第 14 页：大模型降级策略

**三级降级架构：**
```
┌─────────────────────────────────────────┐
│  Level 1: 规则匹配（score >= 0.7）       │ ← 优先
│  - 高置信度，直接返回                    │
│  - 响应时间 < 50ms                       │
└─────────────────────────────────────────┘
                    │ score < 0.7 或 Other
                    ▼
┌─────────────────────────────────────────┐
│  Level 2: Claude API（LLM 诊断）          │ ← 降级
│  - 复杂日志分析                          │
│  - 响应时间 2-5s                         │
│  - API 不可用时→Level 3                   │
└─────────────────────────────────────────┘
                    │ API 失败
                    ▼
┌─────────────────────────────────────────┐
│  Level 3: 通用 fallback                   │ ← 保底
│  - 返回规则模板                          │
│  - 提示用户手动分析                      │
└─────────────────────────────────────────┘
```

**LLM 调用实现：**
```python
def call_llm(exception_type, content, severity, service, stack_trace):
    api_key = os.environ.get("CLAUDE_API_KEY", "")
    if not api_key:
        return None  # 静默降级
    
    try:
        response = _call_claude_api(prompt)
        return parse_llm_response(response)
    except Exception:
        return None  # 异常时静默降级
```

---

### 第 15 页：仪表盘实现

**统计指标计算：**
```python
def get_stats() -> dict:
    logs = repository.get_all()
    
    # 异常类型分布
    exception_counts = Counter(log["exception_type"] for log in logs)
    
    # 每日趋势
    daily_counts = Counter(
        log["created_at"][:10]  # 截取日期部分
        for log in logs
    )
    
    # 严重程度分布
    severity_counts = Counter(log["severity"] for log in logs)
    
    return {
        "total_logs": len(logs),
        "by_exception": dict(exception_counts),
        "by_day": dict(daily_counts),
        "by_severity": dict(severity_counts),
    }
```

**Chart.js 集成：**
```javascript
// 饼图：异常类型分布
new Chart(ctx, {
    type: 'pie',
    data: {
        labels: ['NullPointerException', 'TimeoutError', ...],
        datasets: [{
            data: [25, 15, 10, 5],
            backgroundColor: ['#FF6384', '#36A2EB', ...]
        }]
    }
});
```

---

### 第 16 页：诊断详情页实现

**双栏布局：**
```
┌─────────────────────────────────────────────────────┐
│  原始日志（左栏）        │  诊断结果（右栏）          │
│  ┌──────────────────┐   │  ┌────────────────────┐  │
│  │ ID: abc123...    │   │  │ 严重程度：HIGH     │  │
│  │ 异常类型：NPE    │   │  ├────────────────────┤  │
│  │ 严重程度：HIGH   │   │  │ 根因分析：         │  │
│  │ 服务：user-svc  │   │  │ 对象未初始化...     │  │
│  ├──────────────────┤   │  ├────────────────────┤  │
│  │ 日志内容：       │   │  │ 建议解决方案：     │  │
│  │ java.lang.Null...│   │  │ 1. 检查初始化逻辑  │  │
│  ├──────────────────┤   │  │ 2. 添加 null 检查    │  │
│  │ 堆栈跟踪：       │   │  │ 3. 使用 Optional...  │  │
│  │ at com.example...│   │  ├────────────────────┤  │
│  └──────────────────┘   │  │ 相似日志推荐：     │  │
│                         │  │ - 日志 def456...   │  │
│                         │  └────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**加载流程：**
```javascript
async function loadLogAndDiagnosis() {
    currentLog = await logsApi.getById(logId);      // 1. 加载日志
    currentDiagnosis = await logsApi.getDiagnosis(logId); // 2. 尝试获取诊断
    
    if (!currentDiagnosis) {
        // 3. 无诊断时，调用诊断 API
        currentDiagnosis = await logsApi.diagnose(logId);
    }
    
    renderLog(currentLog);       // 4. 渲染日志
    renderDiagnosis(currentDiagnosis); // 5. 渲染诊断
    showDiagnosis();             // 6. 切换页面状态
}
```

---

### 第 17 页：编辑功能实现

**编辑页面流程：**
```
1. GET /logs/{id} ──→ 回填表单
   ┌──────────────────────────────────────┐
   │ 编辑日志 ID: abc123...               │
   │ ┌────────────────────────────────┐   │
   │ │ 日志内容：[java.lang.NullP...] │   │
   │ │ 异常类型：[NullPointerException▼]│   │
   │ │ 严重程度：( )Low (●)Medium     │   │
   │ │              ( )High ( )Critical│   │
   │ └────────────────────────────────┘   │
   │           [保存] [取消]               │
   └──────────────────────────────────────┘
              │
              ▼
2. PUT /logs/{id} ──→ 更新成功 ──→ Toast ──→ 返回列表
```

**实现代码：**
```javascript
// 加载时回填
async function loadLog() {
    log = await logsApi.getById(logId);
    document.getElementById('content').value = log.content;
    document.getElementById('exceptionType').value = log.exception_type;
    document.querySelector(`input[name="severity"][value="${log.severity}"]`).checked = true;
}

// 保存时提交
async function save() {
    const data = {
        content: document.getElementById('content').value,
        exception_type: document.getElementById('exceptionType').value,
        severity: document.querySelector('input[name="severity"]:checked').value,
    };
    await logsApi.update(logId, data);
    showToast('保存成功', 'success');
    setTimeout(() => window.location.href = 'list.html', 1000);
}
```

---

### 第 18 页：错误处理设计

**错误分级策略：**

| 错误类型 | 前端响应 | 用户感知 |
|---------|---------|---------|
| 页面级 404 | 跳转 404.html | "页面未找到" |
| API 级 404 | 返回 null，调用方处理 | "暂无数据" |
| 服务端 500 | Toast 提示 | "服务器错误，请稍后重试" |
| 网络错误 | Toast 提示 | "网络连接失败" |

**关键实现：**
```javascript
// api.js: 全局错误处理
async function apiRequest(endpoint, options = {}, skip404Redirect = false) {
    const response = await fetch(`${API_BASE_URL}${endpoint}`, options);
    
    if (response.status === 404) {
        if (!skip404Redirect) {
            window.location.href = '404.html';  // 页面级 404
        }
        return null;  // API 级 404
    }
    if (response.status >= 500) {
        showToast('服务器错误，请稍后重试', 'danger');  // 500 不跳转
        return null;
    }
    
    return await response.json();
}

// getDiagnosis 调用时跳过 404 跳转
async function getDiagnosis(id) {
    return apiRequest(`/logs/${id}/diagnosis`, {}, true);  // skip404Redirect=true
}
```

---

## 第四部分：开发流程与最佳实践

### 第 19 页：上下文管理策略

**问题背景：**
- Claude 会话上下文窗口有限
- 长对话中关键信息被挤出
- Token 成本随上下文增长

**五大策略：**

| 策略 | 具体做法 | 工具 |
|-----|---------|------|
| 文件化中间产物 | spec/plan/log 全部落盘 | docs/superpowers/ |
| 任务原子化 | 每 Task 1-3 文件，2-5 分钟 | TodoWrite |
| Git 外部记忆 | Commit 后只引用 hash | git log/show |
| 会话边界管理 | Task 完成立即 commit | git commit |
| 迭代划分 | 按功能模块分 7 个迭代 | 计划文档 |

**文件命名规范：**
```
docs/superpowers/
├── specs/YYYY-MM-DD-<topic>-design.md   # 设计文档
├── plans/YYYY-MM-DD-<topic>-plan.md     # 实现计划
logs/NN-<topic>.log                      # 过程记录
```

---

### 第 20 页：TDD 实践反思

**实际开发中的 TDD 偏离：**

| TDD 正确流程 | 实际执行 | 影响 |
|------------|---------|------|
| ① 先写测试（红） | ① 先写实现 | 测试变成验证工具 |
| ② 验证测试失败 | ② 再写测试 | 无法发现测试无效 |
| ③ 写最小实现 | ③ 验证通过 | 可能过度实现 |
| ④ 重构 | （跳过） | 代码质量下降 |

**改进建议：**
```
正确做法示例（以 LogEntry 模型为例）：

# 步骤 1：先写测试（tests/unit/test_log_entry.py）
def test_log_entry_creation():
    log = LogEntry(content="error", exception_type="NPE", severity="HIGH")
    assert log.id is not None
    assert log.created_at is not None

# 步骤 2：运行测试确认失败
$ pytest test_log_entry.py -v  # FAIL: LogEntry not defined

# 步骤 3：写最小实现（models/log_entry.py）
class LogEntry:
    def __init__(self, content, exception_type, severity):
        self.id = str(uuid4())
        self.content = content
        self.created_at = datetime.now()

# 步骤 4：运行测试验证通过
$ pytest test_log_entry.py -v  # PASS

# 步骤 5：重构（如需要）
```

---

### 第 21 页：Subagent-Driven 开发

**适用场景：**
- 实现计划已明确
- 任务相对独立
- 需要快速迭代

**工作流程：**
```
┌─────────────────────────────────────────────────────────┐
│  1. 读取计划文件，提取任务列表                            │
│  2. 创建 TodoWrite 追踪进度                              │
│  3. for each Task:                                      │
│     ├─► Dispatch 实现 subagent                          │
│     │   └─► 实现者：编码 + 自测 + 自 review              │
│     ├─► Dispatch spec reviewer                          │
│     │   └─► Spec 审查：代码是否符合规格？                 │
│     │       └─► 有问题 → 实现者修复 → 再审               │
│     ├─► Dispatch code quality reviewer                  │
│     │   └─► 质量审查：代码规范、安全性？                  │
│     │       └─► 有问题 → 实现者修复 → 再审               │
│     └─► 双审查通过 → 标记 Task 完成                       │
│  4. 全部完成后，最终代码 review                           │
└─────────────────────────────────────────────────────────┘
```

**优势：**
- 每个子任务 fresh context（无上下文污染）
- 双重审查保证质量
- 自动化流程，减少人工干预

---

### 第 22 页：代码审查清单

**Spec Compliance Review（规格符合性审查）：**
- [ ] 所有规格要求的功能都已实现
- [ ] 没有实现规格之外的功能（YAGNI）
- [ ] API 端点路径、方法、参数与规格一致
- [ ] 数据模型字段与规格一致
- [ ] 错误处理与规格一致

**Code Quality Review（代码质量审查）：**
- [ ] 函数命名清晰（动词 + 名词）
- [ ] 类型注解完整（Python 3.10+）
- [ ] 无未使用的 import 和死代码
- [ ] 错误处理适当（不吞异常）
- [ ] 测试覆盖核心逻辑
- [ ] 无安全漏洞（XSS、注入）

**审查输出格式：**
```markdown
## Spec Compliance: ✅ PASS / ❌ ISSUES

Issues:
- Missing: [缺失的功能]
- Extra: [多余的功能]

## Code Quality: ✅ APPROVED / 🔧 NEEDS FIX

Strengths:
- [优点 1]
- [优点 2]

Issues (Important):
- [重要问题 1]
- [重要问题 2]

Issues (Minor):
- [次要问题 1]
```

---

### 第 23 页：Git 提交规范

**Commit Message 格式：**
```
<type>: <description>

[optional body]

[optional footer]
```

**Type 类型：**
| Type | 说明 | 示例 |
|-----|------|------|
| feat | 新功能 | `feat: add file upload support` |
| fix | Bug 修复 | `fix: handle null severity in diagnosis` |
| test | 测试 | `test: add unit tests for log repository` |
| docs | 文档 | `docs: add README.md` |
| chore | 杂项 | `chore: update requirements.txt` |
| refactor | 重构 | `refactor: extract validation logic` |

**本项目提交历史示例：**
```
95aff83 feat: complete all frontend pages (list, dashboard, diagnosis details)
c027cca chore: fix requirements.txt for Python 3.15 compatibility
478450b docs: add README.md
5e48a98 chore: update requirements.txt and add integration tests
7a277f0 feat: add Docker configuration for containerized deployment
```

**追溯历史命令：**
```bash
git log --oneline              # 查看提交历史
git show <commit-hash>         # 查看具体改动
git diff HEAD~3                # 查看最近 3 次提交差异
git blame file.py              # 查看每行代码的作者
```

---

### 第 24 页：测试策略

**测试金字塔：**
```
           /\
          /  \      E2E 测试（可选）
         /----\
        /      \    集成测试（4 个端点）
       /--------\
      /          \   单元测试（38 个）
     /------------\
```

**本项目的测试覆盖：**

| 测试类型 | 文件数 | 测试用例 | 覆盖内容 |
|---------|-------|---------|---------|
| 单元测试 | 5 | 38 | 模型、服务、工具函数 |
| 集成测试 | 1 | 12 | API 端点、文件上传 |
| **总计** | **6** | **50** | **全部通过** |

**测试文件组织：**
```
tests/
├── unit/
│   ├── test_log_entry.py
│   ├── test_diagnosis.py
│   ├── test_diagnosis_rules.py
│   ├── test_diagnosis_llm.py
│   └── test_file_upload.py
└── integration/
    ├── test_logs_api.py
    └── test_file_upload.py
```

**运行测试命令：**
```bash
# 运行所有测试
pytest -v

# 运行单元测试
pytest tests/unit/ -v

# 运行集成测试
pytest tests/integration/ -v

# 查看覆盖率
pytest --cov=backend -v
```

---

## 第五部分：问题排查与修复实战

### 第 25 页：全栈 Bug 溯源方法论

**核心思想：** 从症状出发，沿调用链逐层追踪，定位第一个偏离点。

**5 步流程：**
```
1. 捕获症状          → 用户实际看到了什么？
       ↓
2. 绘制调用链        → 请求经过哪些层？
       ↓
3. 逐层追踪          → 每层预期 vs 实际
       ↓
4. 定位根因          → 第一个偏离点 = 根因
       ↓
5. 修复并验证        → 最小修复，确认症状消失
```

**关键原则：**
- ⚠️ **停止在第一个偏离点**：之上都是受害者，不是原因
- ⚠️ **不要同时修多层**：无法判断哪个修复有效
- ⚠️ **先追踪再动手**：避免盲目猜测

---

### 第 26 页：Bug 溯源实战案例 1 - 404 跳转

**症状：** 提交日志后跳转到 404 页面

**调用链追踪：**
```
submit.html → submit.js → apiRequest(POST /logs)
    → 后端创建日志 → 返回 log.id
    → window.location = diagnosis.html?id=xxx
    → diagnosis.js → apiRequest(GET /logs/{id}/diagnosis)
    → 后端返回 404（尚无诊断）
    → api.js 全局处理：404 → window.location = '404.html'  ← ROOT CAUSE
```

**逐层分析：**
| 层级 | 预期 | 实际 | 状态 |
|-----|------|------|------|
| submit.js | 提交成功跳转 | 正常跳转 | ✅ OK |
| diagnosis.js | 加载日志和诊断 | 开始加载 | ✅ OK（受害者） |
| api.js getDiagnosis | 404 时返回 null | 404 时跳转页面 | ❌ DIVERGENCE |

**根因：** api.js 全局 404 处理器过于宽泛，将业务 404 等同于页面 404

**修复：**
```javascript
// apiRequest 增加 skip404Redirect 参数
async function apiRequest(endpoint, options = {}, skip404Redirect = false) {
    if (response.status === 404) {
        if (!skip404Redirect) {
            window.location.href = '404.html';  // 页面级 404
        }
        return null;  // API 级 404
    }
}

// getDiagnosis 调用时传入 true
async function getDiagnosis(id) {
    return apiRequest(`/logs/${id}/diagnosis`, {}, true);
}
```

---

### 第 27 页：Bug 溯源实战案例 2 - 序号重复

**症状：** 诊断详情页"建议解决方案"显示 "1. 1. 检查对象..."

**调用链追踪：**
```
后端 diagnosis.py:71
    solution = "\n".join(f"{i+1}. {s}" for i, s in enumerate(rule["solutions"]))
    → 输出："1. 检查初始化\n2. 添加 null 检查\n..."
              ↓
前端 diagnosis.html:109
    <ol id="solution">  ← 有序列表会自动编号
              ↓
前端 diagnosis.js:107
    `<li>${s}</li>`  ← 每个<li>被浏览器再次编号
              ↓
浏览器渲染："1. 1. 检查初始化\n2. 2. 添加 null 检查\n..."
```

**根因：** 后端已带序号 + 前端`<ol><li>`自动编号 = 双重序号

**修复：**
```html
<!-- diagnosis.html:109 -->
<!-- 修改前 -->
<ol id="solution" class="list-group list-group-flush"></ol>

<!-- 修改后 -->
<div id="solution"></div>
```

```javascript
// diagnosis.js:105-109
// 修改前
document.getElementById('solution').innerHTML = solutions.map(s =>
    `<li class="list-group-item">${s}</li>`
).join('');

// 修改后
document.getElementById('solution').innerHTML = solutions.map(s =>
    `<p class="mb-2">${s}</p>`
).join('');
```

---

### 第 28 页：常见根因模式速查

| 模式 | 症状 | 检查位置 | 修复方向 |
|-----|------|---------|---------|
| **全局错误处理过宽** | 正常操作触发错误页 | API 封装层、catch 块 | 分级处理（页面/API） |
| **异步时序问题** | 数据已加载但 UI  stale | setTimeout、遗漏 await | 显式调用状态切换函数 |
| **双重格式化** | 序号重复、双重转义 | 后端 + 前端同时格式化 | 只在一端格式化 |
| **静默失败** | 功能坏但无报错 | try/catch 仅 console.warn | 添加用户可见提示 |
| **端口/配置错误** | Connection refused | API_BASE_URL、server config | 统一配置或释放端口 |
| **编码问题** | 中文乱码 | 文件读写、HTTP 响应 | 统一 UTF-8，errors="replace" |

**调试命令工具箱：**
```bash
# 端口占用
netstat -ano | grep :8000
taskkill //F //PID <pid>

# 进程查看
tasklist | grep python

# 日志查看
tail -f server_debug.log

# Git 追溯
git log --oneline -10
git show <commit>
```

---

## 第六部分：经验教训与总结

### 第 29 页：经验教训汇总

**做得好的地方：**
1. ✅ 文件化所有中间产物（spec/plan/log），避免上下文丢失
2. ✅ 任务原子化，每 Task 独立可验证
3. ✅ 双重代码审查（spec + quality）保证质量
4. ✅ 50 个测试全部通过，测试覆盖率高
5. ✅ Git 提交规范，便于追溯历史

**需要改进的地方：**
1. ❌ 未严格遵循 TDD（先实现后测试）
2. ❌ 任务规划有疏漏（编号不连续，辅助功能未规划）
3. ❌ 代码审查遗留非阻塞问题（XSS 防护、prompt 注入）
4. ❌ 端口管理不当（僵尸进程占用 8000）

**踩过的坑：**
| 坑 | 影响 | 解决方案 |
|---|------|---------|
| API 404 全局跳转 | 新日志提交后跳 404 | skip404Redirect 参数 |
| 异步加载状态 | 页面卡住"加载中" | 显式调用 showDiagnosis() |
| 前后端序号冲突 | 解决方案显示"1. 1." | ol→div，li→p |

---

### 第 30 页：总结与行动项

**核心知识点回顾：**
1. 🏗️ 全栈架构设计：前后端分离 + Repository 模式
2. 🤖 诊断引擎：规则匹配 + 大模型降级三级策略
3. 🐛 Bug 溯源：从症状到根因的 5 步追踪法
4. 📝 上下文管理：文件化 + 原子化 + Git 记忆
5. ✅ TDD 与测试：测试金字塔 +50 个测试用例

**行动项（培训后）：**
- [ ] 在本项目中应用 Bug 溯源方法论
- [ ] 为当前任务创建 spec/plan 文档
- [ ] 实践 TDD 流程（测试→实现→重构）
- [ ] 配置 Git commit message 规范
- [ ] 建立项目 log 文件记录过程问题

**延伸学习资源：**
- FastAPI 官方文档：https://fastapi.tiangolo.com/
- TDD 经典书籍：《测试驱动开发》Kent Beck
- AI 辅助开发最佳实践：待补充

**Q&A**

---

## 附录

### A. 演示脚本

**演示 1：提交日志并诊断（3 分钟）**
```
1. 打开 http://localhost:3000/submit.html
2. 上传 sample-error.log 文件
3. 填写表单，提交
4. 观察跳转到 diagnosis.html
5. 查看诊断结果（根因 + 解决方案 + 相似日志）
```

**演示 2：Bug 溯源过程（5 分钟）**
```
1. 展示测试问题.log 中的问题描述
2. 调用 chain 绘制（白板）
3. 逐层追踪（Read 文件）
4. 定位根因
5. 应用修复
6. 运行验证
```

### B. 代码片段索引

| 功能 | 文件 | 行号 |
|-----|------|------|
| API 请求封装 | frontend/js/api.js | 10-45 |
| 文件上传处理 | backend/routes/logs.py | 15-60 |
| 规则匹配引擎 | backend/services/diagnosis_rules.py | 全文 |
| LLM 降级调用 | backend/services/diagnosis_llm.py | 46-75 |
| 404 分级处理 | frontend/js/api.js | 22-26 |
| 诊断页面加载 | frontend/js/diagnosis.js | 33-70 |

### C. 检查清单

**开发前：**
- [ ] 读取最新 plan 文档
- [ ] git status 确认无遗留
- [ ] 确认当前 Task 编号

**开发后：**
- [ ] 运行相关测试
- [ ] git commit（规范 message）
- [ ] 更新任务进度记录

**发布前：**
- [ ] 所有测试通过
- [ ] 代码审查完成
- [ ] 文档更新完成
